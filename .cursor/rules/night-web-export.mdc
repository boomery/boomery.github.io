---
description: 推送 NightEscort 的 main 后自动导出 Web 到博客 /night/
alwaysApply: true
---

# 夜行镖 Web 自动导出

游戏源码仓库是 `https://github.com/boomery/NightEscort.git`，分支 **`main`**。两台电脑是同一套逻辑，只是源码目录不同：

- Mac：`~/Desktop/night`（这台机器上是 `/Users/chaoxinzhang/Desktop/night`）
- Windows：`~/Documents/GitHub/NightEscort`（这台机器上是 `C:\Users\MyPC\Documents\GitHub\NightEscort`）

只要这次对话改了 **Godot 游戏源码**（脚本、场景、资源，不是已经导出的 `web/`），完成改动后必须：提交并 `git push origin main`。不要让用户自己点导出，也不要再问要不要推源码。

推送 `main` 时，源码仓库的 `.githooks/pre-push` 会调用本仓库的 `scripts/export-night-web.sh`：无头导出 Web，把 `night.js` / `night.wasm` / `night.pck` 同步到 `myblog/source/night/`（不覆盖自定义封面，只改 `fileSizes`、版本号和更新时间），上传 pck 到 R2，然后提交并推送本仓库 `master`。

看那次 `git push` 的输出：

- `NIGHT_WEB_EXPORT_OK`：导出和博客推送已完成。把源码和博客的提交哈希告诉用户，不要再导第二次
- `NIGHT_WEB_EXPORT_FAIL`：源码推送仍会继续。把失败原因告诉用户，不要自动连着重试整次导出
- 没有这两行，也没有「跳过 Web 导出」：这台机器还没启用钩子。在源码仓库执行 `git config --local core.hooksPath .githooks` 后再推一次。钩子仍没跑时，才在本仓库根目录手动执行 `bash scripts/export-night-web.sh`

本机若还没执行过上面的 `core.hooksPath`，先写上再推送。

Godot：Windows 用 `F:\Backup\桌面\夜行镖\Godot_v4.7.2-stable_win64.exe\Godot_v4.7.2-stable_win64_console.exe`（4.7.2 stable，这个目录名本身是文件夹）。Mac 用 `~/Downloads/Godot.app`。无头导出、跑测试都用脚本里写好的路径，不要再在本机搜索别的 Godot。

本地预览、先不传 CDN 时用 `bash scripts/export-night-web.sh --skip-r2`。这不走钩子，也不会自动提交。

## 不要做的事

- **不要**用 Godot 生成的 HTML 覆盖 `myblog/source/night/index.html`（封面、进度条、印信 `NightSave` 都在这份自定义页里）
- **不要**把 `night.pck` 提交进任一 git（已 gitignore；走 R2）
- **不要**把 `web/` 导出目录提交进 NightEscort（那是本地导出产物，线上以博客 `/night/` + R2 为准）
- 只改博客 HTML/CSS/印信桥、没动 Godot 源码时，**不要**跑导出，也**不要**空提交 NightEscort

## 导出之后

钩子成功时，`night.wasm` / `night.js` / `index.html` 的体积数字和全站右下角时间会随博客 `master` 一起推送。pck 只在 R2。

## 左下角更新时间

只要这次改动会发布到 `/night/`（封面、`night.js`、样式、印信桥，或上面的 Godot 导出），提交前把页面左下角时间改成当前东八区时间，格式 `YYYY-MM-DD HH:mm`。两处一起改：`<time id="nightBuiltAt">` 和 `const NIGHT_BUILT_AT`。Godot 导出脚本会写这两处；只改博客、不跑导出时也要手改。游戏版本号仍只用 `project.godot` 的 `config/version`。
